<%= content_for :breadcrumb do %>
  <%= print_breadcrumbs([
                      ["#{@customer.title}", customer_slips_path(@customer)],
                      [t('breadcrumb.estimates.new', :default => 'New estimate'), nil]
                    ]).html_safe %>
<% end %>

<h1><%=t '.title', :default => "New estimate" %></h1>

<%= render 'form' %>
