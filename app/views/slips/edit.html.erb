<%= content_for :breadcrumb do %>
  <%= print_breadcrumbs([ 
                      ["#{@customer.title}", customer_slips_path(@customer)],
                      [t('breadcrumb.slip.edit', :default => "Edit slip"), nil]
                    ]).html_safe %>
<% end %>

<h1><%=t '.title', :default => "Edit slip" %> <%= @slip.name %></h1>

<%= render 'form' %>