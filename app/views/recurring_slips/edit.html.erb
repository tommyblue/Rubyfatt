<%= content_for :breadcrumb do %>
  <%= print_breadcrumbs([ 
                      ["Recurring slips", recurring_slips_path],
                      ["Edit recurring slip", nil]
                    ]).html_safe %>
<% end %>

<h1>Edit recurring slip</h1>

<%= render 'form' %>