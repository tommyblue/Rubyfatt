<%= content_for :breadcrumb do %>
  <%= print_breadcrumbs([
                      [t('breadcrumb.consolidated_taxes.index', :default => 'Consolidated taxes'), consolidated_taxes_path]
                    ]).html_safe %>
<% end %>
<%- model_class = ConsolidatedTax.new.class -%>
<h1><%=t '.title', :default => model_class.model_name.human.pluralize %></h1>
<% @consolidated_taxes.each do |consolidated_tax| %>
  <h3><%= consolidated_tax.name %></h3>
  <table class="table table-striped table-bordered table-condensed">
    <thead>
      <tr>
        <th><%=t model_class.human_attribute_name(:order) %></th>
        <th><%=t model_class.human_attribute_name(:name) %></th>
        <th><%=t 'value', :default => "Value" %></th>
        <th><%=t Tax.new.class.human_attribute_name(:compound) %></th>
        <th><%=t 'actions', :default => "Actions" %></th>
      </tr>
    </thead>
    <tbody>
      <% consolidated_tax.taxes.each do |tax| %>
        <tr>
          <td><%= tax.order %></td>
          <td><%= tax.name %></td>
          <td>
            <%= tax.rate %>
          </td>
          <td>
            <%= tax.compound ? '<span class="label label-success"><i class="icon-ok"></i></span>'.html_safe : '<span class="label label-important"><i class="icon-remove"></i></span>'.html_safe %>
          </td>
          <td>
            <%= link_to '<i class="icon-edit"></i> '.html_safe+t('.edit', :default => "Edit"), edit_consolidated_tax_tax_path(consolidated_tax, tax), :class => "btn btn-mini btn-warning"%>
            <%= link_to '<i class="icon-remove"></i> '.html_safe+t('.delete', :default => "Delete"), consolidated_tax_tax_path(consolidated_tax, tax), :method => :delete, :confirm => t('.delete_tax_confirm', :default => "Are you sure to delete the tax?"), :class => "btn btn-mini btn-danger"%>
          </td>
        </tr>
      <% end %>
      <tr>
        <td colspan="4">
          <%= link_to t('.new_tax', :default => "New tax"), new_consolidated_tax_tax_path(consolidated_tax), :class => "btn btn-primary" %>
        </td>
      </tr>
    </tbody>
  </table>
<% end %>

<% link_to t('.new', :default => t("helpers.links.new")),
            new_consolidated_tax_path,
            :class => 'btn btn-primary' %>
<a class="btn btn-primary disabled" href="#"><%=t '.new' %></a>
