<%= content_for :breadcrumb do %>
  <%= print_breadcrumbs([ 
                      ["#{@customer.title}", customer_slips_path(@customer)],
                      ['New invoice', nil]
                    ]).html_safe %>
<% end %>
<h1>New invoice</h1>

<%= render 'form' %>