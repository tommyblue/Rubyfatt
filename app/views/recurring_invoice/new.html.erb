<%= content_for :breadcrumb do %>
  <%= print_breadcrumbs([ 
                      ["Recurring slips", recurring_slips_path],
                      ["New recurring invoice", nil]
                    ]).html_safe %>
<% end %>

<h1>New invoice from recurring slip</h1>

<%= render 'form' %>