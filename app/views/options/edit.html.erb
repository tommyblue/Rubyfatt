<%= content_for :breadcrumb do %>
  <%= print_breadcrumbs([ 
                      ["Options", options_path],
                      ["Edit option", nil]
                    ]).html_safe %>
<% end %>
<h1>Edit option "<%= @option.name %>"</h1>

<%= render 'form' %>